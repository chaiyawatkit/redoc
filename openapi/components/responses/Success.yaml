Update:
  description: Success
  content:
    application/json:
      schema:
        type: object
        additionalProperties: true
        minProperties: 1
        description: >-
          The Success Details JSON Object.
        properties:
          status:
            type: integer
            description: The HTTP status code.
            example: 200
          message:
            type: string
            example: update success message
Delete:
  description: Success
  content:
    application/json:
      schema:
        type: object
        additionalProperties: true
        minProperties: 1
        description: >-
          The Success Details JSON Object.
        properties:
          status:
            type: integer
            description: The HTTP status code.
            example: 200
          message:
            type: string
            example: delete success message
Create:
  description: Success
  content:
    application/json:
      schema:
        type: object
        additionalProperties: true
        minProperties: 1
        description: >-
          The Success Details JSON Object.
        properties:
          status:
            type: integer
            description: The HTTP status code.
            example: 200
          message:
            type: string
            example: create success message
          